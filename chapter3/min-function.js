// Min function
function min (n1, n2) {
  minNumber = Math.min(n1, n2);
  return console.log(minNumber);
}

// min(100, 30);

// Arrow function
min = (n1, n2) => {
  minNumber = Math.min(n1, n2);
  return console.log(minNumber);
};

min(10, 30);
